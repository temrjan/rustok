[package]
name = "rustok-core"
description = "Core wallet logic — keyring, provider, router, explainer"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
txguard = { path = "../txguard" }
rustok-types = { path = "../types" }

# Ethereum
alloy-primitives = { workspace = true }
alloy-provider = { workspace = true }
alloy-transport-http = { workspace = true }
alloy-rpc-types-eth = { workspace = true }
alloy-network = { workspace = true }
alloy-consensus = { workspace = true }
alloy-signer = { workspace = true }
alloy-signer-local = { workspace = true }
alloy-chains = { workspace = true }
alloy-eips = { workspace = true }

# Crypto (key encryption)
aes-gcm = "0.10"
argon2 = "0.5"
rand = "0.8"
zeroize = { version = "1", features = ["derive"] }

# HTTP (shared client for RPC providers — must match alloy-provider's reqwest version).
# `rustls-no-provider` activates __rustls without auto-installing a platform verifier;
# we supply webpki-roots + ring via ClientBuilder::use_preconfigured_tls in http.rs.
reqwest = { version = "0.13", default-features = false, features = ["json", "rustls-no-provider"] }
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
webpki-roots = "1"

# Async — `sync` feature additive over workspace base (rt-multi-thread, macros)
# for `tokio::sync::Mutex` used by the wallet service.
tokio = { workspace = true, features = ["sync"] }
futures = { workspace = true }

# QR rendering for wallet receive screen (EIP-681 URI → SVG).
qrcode = { version = "0.14", default-features = false, features = ["svg"] }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Error handling
thiserror = { workspace = true }

# Logging
tracing = { workspace = true }
# Diagnostic build for Issue #15 (createWallet crash on Xiaomi MIUI).
# Temporary dep for one-shot runtime check log — remove after root cause is identified.
log = "0.4"

[dev-dependencies]
tokio = { workspace = true, features = ["test-util"] }
tempfile = "3"
