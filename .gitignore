# Rust
/target/
**/target/
**/*.rs.bk
# Cargo.lock коммитим — есть бинарники (cli, api)

# Tauri / Trunk build artifacts — commit gen/android/ (user-modified Gradle for rustls JNI).
app/dist/
app/src-tauri/gen/schemas/
app/src-tauri/gen/apple/
app/src-tauri/gen/android/app/build/
app/src-tauri/gen/android/.gradle/
app/src-tauri/gen/android/local.properties

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Android signing secrets — never commit
*.jks
*.keystore
keystore.properties

# Play Store assets (generated locally, not source-of-truth)
play-assets/

# Internal docs (not public)
docs/internal/

# Node (workspaces — root + workspace packages)
node_modules/

# Node (Cloudflare Worker)
deploy/rpc-proxy/node_modules/

# Session artifacts (debug screenshots from device — not source-of-truth)
/*.jpg

# Dev artifacts (ephemeral testing / coverage / local harness — not source-of-truth)
smoke.log
coverage/
.claude/
/nul
